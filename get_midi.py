import urllib.request
import json
import base64

# Let's search for a midi file of La Campanella or just generate a sequence
# Actually I can use an existing API or just write a small python script that generates the campanella theme.
